# Sólo para pruebas LOCALES (python -m realestate.cli scan --dry-run, o un
# envío real de prueba). En producción estas credenciales van como Secrets
# del repo de GitHub (Settings → Secrets and variables → Actions), nunca en
# este archivo commiteado. Ver docs/DESPLIEGUE.md.

GMAIL_USER=
GMAIL_APP_PASSWORD=
